machine:
  ruby:
    version: 2.1.5
        
test:
  override:               
    - rake all    
    
  post:  
    - ls
    - cp -R doc $CIRCLE_ARTIFACTS/
    - cp -R coverage $CIRCLE_ARTIFACTS/
    - cp *.gem $CIRCLE_ARTIFACTS